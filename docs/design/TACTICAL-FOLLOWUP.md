# Tactical / economy / negotiation follow-up

Same originating FIELDLINE project. User explicitly repeats the implementation request (including "거래 화면 리뉴얼 하라고 했잖음"); proceed with the consolidated additions rather than another approval round. Earlier unsent confirmation drafts are not delivered requests. No generated imagery; original code-native design remains authoritative.

Scope: fix city/garrison identity and static city rendering; right-click selected unit cancels only future route (keep left-click); no-attack own-turn-end healing even after movement; city/artillery range2; builder forts with HP and stationed allied bonus; persistent last-seen city silhouettes without live hidden state; completion prompts at next actionable own turn, sequential for multiple cities; optional idle production increases food. Expose iron/horses/niter quantities and incomes directly in topbar. Move faction emblems to upper-right beside diplomacy/market and separate opaque layer toggle from board edge.

Numeric implementation defaults: fort80HP, stationed defense+25%, one builder charge/all remaining MP, only empty own non-mountain land (no city/farm/resource facility overlap); fort receives damage when its occupant is attacked or when directly targeted empty, bonus ends at destruction. Idle city food+25% rounded up before supply penalty. Recovery remains8HP per formation member, no recovery when isolated or merging. All must be shared between prediction/UI/server/NPC and legal player-scoped APIs.

Trading redesign: two clean equal inventory columns, each with persistent faction heading, category tabs and chosen terms separated from available items; no raw select dialog. True white surface, existing teal/ink palette, native SVG, 14px body/12px help, measured button sizes. Persistent bottom negotiation footer: live NPC accept/reject/human-consent status, exact additional gold when possible, "뭘 원하십니까?" applies offered counterterms, review then submit. Server read-only quote uses same validation/valuation as actual deal, no escrow/mutation and no private inventory counts. Human players must explicitly accept; never fake their judgment. Category controls collapse content, not deal capabilities: cities/units/resources/gold/alliance/either-side third-party war remain.

Desktop trade bounded~860px bottom dock with independent inventory scrolling and persistent terms/status; mobile side-by-side compact columns with readable rows, no horizontal page overflow. Header resources get a compact row on small screens; controls remain opaque and separated from map/clock. No additional decorative dashboards or permanent map inspector. This code-native spec replaces concept images by explicit user opt-out.

Verify: entity-ID uniqueness/static city matrices; cancel leaves MP; no-attack healing and attack/supply exceptions;2 allowed/3 rejected; fort charge/HP/bonus/destruction; idle food and durable production prompts; stale city ownership/HP hidden until revisited; real quote/execution consistency, empty/private/human/war cases. Then browser real right-click, production/skip, city memory, range/fort UI, live quote/counteroffer and actual trade, desktop/mobile screenshots using agent-browser. Preserve baseline74-test checkpoint; backend restart must be disclosed because matches are in memory.
